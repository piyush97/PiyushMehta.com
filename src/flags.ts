/**
 * Build-time feature switches. Flip value follows — no
 * component hunting. Keep file dependency-free so client islands can
 * import safely.
 */

/** Newsletter is live: page, nav/footer links, blog CTAs. */
export const NEWSLETTER_ENABLED = true;
