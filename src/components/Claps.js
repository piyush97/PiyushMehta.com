import React from 'react';

const Claps = () => {
  return (
    <applause-button
      color="var(--xstyled-colors-lighter)"
      multiclap="true"
      style={{
        width: '5rem',
        height: '5rem',
        display: 'block',
        marginTop: '4rem',
      }}
    />
  );
};

export default Claps;
